# Jotla redesign, August 2026: the neutral shell

Direction set by the founder on 2026-08-06 after reviewing Todoist's dark Android app (his reference screenshots live in Vision, `Gemini Gems/Demo`). The board that carries every decision below is [`lookdev-002.html`](lookdev-002.html): open it in a browser, dark pair on top, light pair below, Menu icon options and the D/G/E nav comparison above the frames.

**Status: dark theme LOCKED. Light theme first pass. THE WEB BUILD SHIPPED 7 Aug 2026 and iterated to 2.0.11** (sw v2.0.11, boot-assert 282/282 green with proven negative controls, live at https://okbupe.github.io/jotla/). `art/plus-6.jpg` (the Mood Styles slide) LANDED the same night: Bupe generated it on Higgsfield (nano_banana_2, 2K, 16:9, generation ed3556f1) from the handed-over prompt; pixels verified against the brief (cat, classic, bear badges, tint disc, single shadow, sparkle + veined leaf, no text) and resized to the set's 720x402 jpg class. The native mirror is the outstanding half of the two-track rule (its Plus ladder already matches by coincidence of history: £49 / £79, no monthly; the tile-sub recolour is mirrored natively; the nav animations and the 2.0.7 speed dial are not yet, and 2.0.7 retires the native Today-tiles pattern conceptually too).

**2.0.11, the roster cut to two (Bupe, 9 Aug late: "all the same kinda").** Six of 2.0.10's packs died hours after shipping: Soft, Flat, Bubble, Outline and both cat packs were smiley-recolours of one idea, and the founder wants genuine variety (his reference shows character packs: cat, chick, ghost, dog, frog, fluff, puzzle, robot). What stays: **Bold (Twemoji) is now the DEFAULT and free** (the drawn classic smiley retired with the cut), and **Sticker (Fluent 3D) is the one Plus look**. Stale prefs from any earlier roster migrate to Bold; the tour's child slide now wears the Twemoji face. LICENSES.md and the About credits trimmed to the two sets. Character packs with real variety are being chosen separately (research handed to the founder) and will join as Plus looks once picked. Suite 282/282, the default negative-controlled (flipping it fails two asserts).

**2.0.10, real emoji packs and the Mood style page (Bupe, 9 Aug: "proper emojis, already created... a new page showing you how they look").** The 2.0.9 code-drawn Cat and Bear are RETIRED (founder verdict: bad and basic). Mood styles v2 bundles REAL emoji artwork from openly licensed sets, nine packs of the app's five moods: Classic (ours, free, drawn), Sticker / Soft / Flat (Microsoft Fluent 3D, Color, Flat: MIT), Bubble (Google Noto: Apache 2.0), Bold (Twemoji: CC BY 4.0), Outline (OpenMoji: CC BY-SA 4.0), Sticker Cat (Fluent 3D cat faces) and Bubble Cat (Noto cat faces), 40 files under `moods/`, every URL fetched 200 and none empty, notices in `moods/LICENSES.md` plus a credits paragraph on About. The picker is now a FULL PAGE (`moodstyle` route, back chevron): each pack is a card showing its five moods in a row, blue tick on the active pack, crowns on the eight Plus packs for free users with the crown gate opening the paywall; owners tap to apply instantly, app-wide via `window.JOTLA_FACE_STYLE`, persisted in prefs, with unknown pack names falling back to Classic so a stale pref can never blank a face. The sheet version is gone. Slide 6's caption now says stickers, bubbles or cats (no bear promise). Suite 281/281: nine pack assertions including the 45-preview count, negative-controlled by dropping the prefs write (1 FAILED).

**2.0.9, a paid app stops selling, and the faces get looks (Bupe, 8 Aug night, four calls).** One: the crown gate now exists ONLY in the free app: an owner's Backup rows carry no crowns (the `crown` const nulls on nav.plus, with a `data-crown-gate` hook so the suite counts six on free, zero on paid). Two: a paid-up Menu carries no advertising: the Arriving-2027 AI ticket is gone behind `AI_AVAILABLE = false` and returns in that slot only when Jotla AI actually ships. Three: the Settings Membership row moved to the very bottom (a receipt, not a feature; its crown stays as the tier emblem). Four: **Mood Styles**: Settings > Appearance gains a Mood style row and sheet: Classic (free, the current smileys) plus Cat (ears + whiskers) and Bear (round ears), both drawn in the Face component's own SVG (same five moods, same warm palette, shape-only additions so both themes hold), crown-gated on free (a gated tap opens the paywall), free choice for owners, applied app-wide through `window.JOTLA_FACE_STYLE` and persisted in prefs. The Plus carousel gains slide 6 (Mood Styles); its art slot awaits Bupe's Higgsfield render. Suite 278/278: nine new assertions, negative-controlled (crown gate ungated + AI_AVAILABLE flipped: 2 FAILED; Face ignoring the style global: 1 FAILED).

**2.0.8, the door learns a knock (Bupe, 8 Aug night, two calls).** One: the three capture screens (Quick log, Add a document, Dysregulation's free explainer) drop the top-right X for the Settings-style back chevron, one consistent way out, top left; the guided handover already had it, and the paywall keeps its X by design. The moment-editor suite exits were riding the old X and now ride the chevron. Two: **a double tap on the + fires Quick Log directly** (the most-used capture anywhere): the first tap opens the dial as normal, a second tap inside 320ms fires, with the tap clock on a ref per the 7 Aug swipe lesson. A quiet pill above the + teaches it ("Try a double tap for Quick Log") and retires FOREVER on the first successful double tap, persisted in `jotla_fabtip_v1`, never to nag again. Suite: 269/269 with six new assertions (three chevron-not-X, tip-shows-fresh, double-tap-fires, tip-retires-with-flag), negative-controlled by dropping the persistence write and reverting one chevron (exactly 2 FAILED).

**2.0.7, one door for everything: the + speed dial (Bupe, 8 Aug night, the Drive pattern).** The + is now the app's single capture door. Pressing it blurs the whole app behind a scrim (backdrop-filter, tab bar included), morphs the + into an x (45deg, sprung), and staggers in four pill options, Quick Log nearest the thumb, then Child's Day, Dysregulation, Document. Today's two check-in tiles RETIRED into it (Today now leads with the day itself: greeting, date, then "[child]'s day so far"), and Documents' dashed "Add a document" row retired with them, all three jobs living behind the one +. Tier routing preserved: Dysregulation dials into the guided handover on Plus and the explainer on Free; the tiles' do-it-together vs hand-the-phone copy nuance lives on inside child mode. The tour's slides 2, 3 and 5 rewrote to point at the plus (they described the dead layout). The ActionTile component, the j-ctile CSS family and the tile vars (including 2.0.6's sub vars) were REMOVED, not orphaned; 2.0.6's tile-sub contrast assertions retired with them and the suite gained six dial assertions instead (exact option order, live blur, the x morph, scrim-close, Quick Log routing, the dashed row's absence), negative-controlled (blur off + an option renamed: exactly 2 FAILED). A scrim tap closes; prefers-reduced-motion drops the stagger and the morph but keeps the dial.

**2.0.6, the Today board and the nav's own voices (Bupe, 8 Aug evening).** Three founder calls in one round. The check-in tiles' sub-lines drop the neutral grey for a muted shade of their own hue (new vars `--tileblue-sub` / `--tilepurple-sub`, both themes): the grey was clashing on the tinted panels, and it measured 3.93/4.03 contrast, under the 4.5 floor; the replacements measure 4.81/5.0 and the suite now asserts both the two-hue truth and the contrast floor. The tile icons go 23 to 28. And every tab-bar icon answers the press with its OWN micro-animation before settling into the blue active state: today breathes, the calendar flips its page, the document lifts, the magnifier sweeps, the menu pops; ~0.3s, transform-only, class cleared on animationend, all off under prefers-reduced-motion. The suite presses all five and asserts five distinct animation names, then their disappearance (negative-controlled by breaking the wiring: five nones, 1 FAILED). Native carries the tile-sub mirror; the nav animations join the native mirror pass.

**2.0.5, the two-term ladders (Bupe, 8 Aug, acting on the first /arena verdict).** Both tiers lose the monthly rung: Plus is £49 / £79 (6 Months / One Year), and Jotla AI's indicative ladder is £89 / £149 with Plus included, cut from £199 after the arena put every live AI-inclusive comparable at or under about £120/yr. The renewal small print drops the "at least 24 hours before the term ends" clause, an Apple convention that never applied on Google Play (Google's own help: cancel any time, access runs to the end of the paid term), and the AI face now states its prices are indicative until set at launch. The suite gains four paywall assertions (both ladders, the absences, the Google-accurate cancel line); lookdev-002's pricing panels, reassure blocks and FAQ synced the same day. Arena record: Vision `projects/sen-help/SEN Help Resources/App/Jotla-Arena-Price-Ladder-2026-08-08.md`.

**2.0.4, the Careful rows (Bupe, 7 Aug).** The two rows on the child profile page lose their grey sub-lines: "Reset this child" and "Delete this child" stand on their titles, and the guarded sheet carries the detail. The guarded sheet itself was trimmed so its TALL case fits: delete shows a third consequence row (the child's profile) and was scrolling on a standard phone at 799px of content against a 765px cap, while reset fitted at 732. The warn stage now measures 682 (reset) and 743 (delete), both in full, no scroll. Nothing was removed from the warning: the shield disc went 56 to 44, consequence rows 13px to 10px of vertical padding, and four margins came in by 4 to 6px each. The suite pins both at 390x844 with a taller-of-the-two control. **Small print, and it is real:** at 375x667 and at 360x780 both sheets still scroll, because the warning is longer than those screens. Trimming further means cutting warning copy, which is a founder call, not a layout one.

The 2.0.2/2.0.3 morning round (founder calls, 7 Aug): the settings glyph became a real toothed cog (the ray version read as a sun); every tab shares ONE measured title line (pads unified at 10, corner buttons in height-capped boxes, Today's greeting at the same 28px as the other tabs, suite asserts a <=1.5px spread and measures 0.0); pushed-page titles match at 28px with the tiers header staying small; the Jotla AI ladder was reset by Bupe to £79 / £129 / £199 ("less than £17 a month"); and the Jotla AI mark is now the solid twin-sparkles glyph, the app's second solid icon beside the crown.

The 2.0.1 polish round (all founder calls, evening of 7 Aug): per-type document icons echoing the pill colours; the document page gained Print or save as PDF (documents stay OUT of the day-record PDF, an open question for Bupe); the FAB steps aside on Day records; tab titles centre on the corner-icon line; a Plus owner's Menu ticket sells Jotla AI (navy/gold) while the owned tier sits in Settings > Membership as an Active row; the paywall carousel finger-swipes on a pointer-event rail (gesture state in refs, never state closures: closures drop fast flicks), with the lockup, caption and dots pinned so nothing moves across tiers or slides, and the corner crown wearing the open tier's colours; Plus terms end "It never touches your history."

## The one-line direction

The shell goes neutral and the data gets the colour back. One accent (Jotla blue) does all the shell work; the mood colours are the only other colour on screen, so meaning is the brightest thing.

## Rules (all decided 2026-08-06, Bupe)

- **Rows, not cards.** Flat full-width rows on a slightly lighter surface, 14px radius, no card-in-card, no shadows doing border work. Light theme uses a hairline border instead of elevation.
- **One big left-aligned title** per screen ("Good afternoon.", "August 2026"). No app header bar; the wordmark's homes are the splash and About (per the 4 Aug declutter).
- **Section headers** are small, semibold, accent-coloured ("Check in", "Sam's day so far").
- **FAB**: a circle, accent fill, bottom right. It replaces BOTH the centre nav "+" and the full-width "Add to today" button. One add affordance.
- **Nav**: five tabs, `Today / Month / Documents / Find / Menu` (Find placed between Documents and Menu, locked 2026-08-06 after trialling it against a corner icon). Original icon set (today house, calendar, doc, search). Active state is colour only (accent, stroke 2.2 vs 2). No pill or background behind the active icon.
- **Menu icon**: four rounded squares (option D on the board; picked over sliders G and dots E in real nav context). Menu holds the child switcher, Settings, export, About, and the "by SEN Help" endorsement.
- **Find**: lives in the nav (fifth tab). The earlier top-right corner icon is retired; screen headers carry no search.
- **Month grid**: day tints are circles; the current day carries a 1.5px accent ring; tints appear only on days that hold data.
- **The pulse icon is the standing Jotla symbol for dysregulation** (path `M3 12h4l2.5-7 4 14 2.5-7H21`, drawn in the set's grammar).
- **Pill stroke law holds**: every tinted chip carries a 1px same-hue border (Dysregulation chip, Plus chip).
- **The crown is the Plus gate, app-wide (Bupe, 6 Aug).** In the FREE app, a Plus-gated row shows the solid gold crown in place of its trailing control (chevron or toggle), with no "Plus" pill or label needed, and **tapping any crowned row always opens the Jotla Plus page**. In the paid app the crown disappears and the real control appears. This replaces the old PlusLockedCard pattern everywhere it survives the redesign (Month patterns, Find filters, media locks, the vault's add-the-document).
- **No trailing arrows on rows, app-wide (Bupe, 6 Aug).** A row is tappable as a whole; nothing decorates its right edge. The only trailing marks are live values (Theme's current value, backup status) and the gold crown on Plus rows in the free app. Month's previous and next buttons are pagers, not row furniture, and keep their arrows.
- **Bottom sheets (pattern set by the export flow, 6 Aug).** The shell's modal: a scrim over the screen, a surface-coloured sheet rising from the bottom with a 22px top radius, a grab handle, a Cal Sans title at 20px, one grey sub-line, radio option rows split by hairlines, and a full-width accent-fill action button. **Export my data opens the period sheet**: The whole record (default) / Last 7 days / Last 30 days / Choose dates, which expands From and To date fields; Export is the one confirmation tap. Every period is free, no crowns in this sheet: the free-export promise covers any slice of the record. (Date ranges on the PDF Evidence Pack stay a separate Plus candidate.)
- **Children and the child profile (Bupe, 6 Aug evening, superseding the earlier single child page).** `Children` is the FIRST row in Settings (un-labelled at the top, like the live app's settings child card; sub lists the children). It opens the **Children page**: each child as a row (avatar, name, "Year 2 · Oakfield Primary" sub) with an accent tick on the active child, plus "Add another child" as a dashed row; tapping a child opens their profile. The **child profile** opens like a profile screen: back chevron only, then the child's image BIG and central (88px), name in Cal Sans under it, year and school under that, and the photo pill ("Upload a photo"; with a photo set it becomes "Change photo" plus "Remove", and upload goes through the live app's crop step: same controls, same functionality). Then Details as value rows: Name, School, Year, The adults around them, and **Colour and avatar**, which opens a sheet. Careful section (hard colour): **Reset this child** and **Delete this child** (renamed by Bupe from the earlier labels), both keeping the live app's backup-first confirm sheets.
- **The Colour and avatar sheet: 14 colours, 14 avatars, Done and Cancel (Bupe, 6 Aug).** Colour: the live app's 12 (`AVATAR_COLOURS`: amber, gold, orange, lime, green, teal, sky, indigo, slate, violet, plum, pink) plus two new hues the build must add: **Lavender `#9C8CEC`** and **Cocoa `#A9744F`** (Mint and Cyan were tried and dropped: too close to green and teal, and 14 matches the avatar count). Still no red anywhere: red stays the alert colour. Sky is the default. Avatar: the live 10 glyphs (person, heart, star, leaf, sparkle, shield, bell, hand, house, note) plus four additions: the child's **initial** (the redesign default) and three new glyphs for the build, **moon, sun, music**. **The two grids are identical geometry**: 37px circles, 7 per row, two rows each, an even 15px gap, columns aligned. **This is the shell's third sheet type, the edit sheet**: taps preview live on the big avatar, **Done** commits and updates the row value, **Cancel** or the scrim reverts everything. (Pickers apply instantly; action sheets carry one button; edit sheets carry Done and Cancel.) **Avatars app-wide render as white initial-or-glyph on the chosen figure colour**, replacing the earlier accent-tint letter discs.
- **The Theme picker is a radio sheet** (Bupe, 6 Aug): Light / Dark / System, System carrying the sub-line "Follows your phone". A tap applies instantly and updates the Theme row's value; picker sheets carry no confirm button, action sheets (Export) carry exactly one.
- **Check in is two tiles side by side** (Bupe, 6 Aug): Your day on a deep blue panel, Dysregulation on a deep plum, dedicated `--tileblue` / `--tilepurple` tokens sitting deeper than the chip tints, icon and title in the tile's colour. The tiles carry a drop shadow plus a slight same-hue stroke (1px at soft alpha: Bupe first removed the stroke for shadow-only, then brought a lighter one back on 6 Aug); they are the app's only shadowed surfaces. The check-in actions are the one place the shell lends colour to controls, because they are the doors to the data itself.
- **The Month patterns lock is a blurred preview below the calendar** (Bupe, 6 Aug): the analytics shape blurred behind a soft veil carrying the solid gold crown, "Month patterns" and the feature line. The shape is two graphs: the mood bars, then a shorter purple row underneath for the dysregulation dimension, then the counts line ("9 good · 4 mixed · 5 hard · 6 dysregulation"). The old Plus row above the calendar is gone. Tapping the preview opens the Jotla Plus page (crown gate).
- **Document types are colour-coded pills** in the vault row sub-line (Bupe, 6 Aug): Plan in accent blue, Letter in amber, Email in green, pill stroke law applied. First mapping, open to reshuffle; Letter currently shares amber with the action chip.
- **The Plus page's "Everything in Free is included, always." sits between the CTA and the renewal text** (Bupe, 6 Aug), centred with its check mark.
- **Type: screen titles are Cal Sans, Regular, never bold** (Bupe, 6 Aug; Cal Sans only ships one weight, which is the point). Body stays Outfit.
- **Two faces only, enforced (Bupe, 6 Aug):** everything in the app renders in Outfit except display titles in Cal Sans. Buttons do not inherit font-family by default, so the phone scope carries `button { font-family: inherit }`; without it the nav labels and segmented control were silently falling back to the system font. Verified by a computed-font sweep across every frame: zero offenders.
- **Text size is a radio sheet like Theme** (Bupe, 6 Aug): Small / Standard / Large / Extra large, each label previewing its own size; a tap applies instantly and updates the row's value.
- **The paywall carries both tiers behind a selector (Bupe, 6 Aug).** The Documents segmented slider sits in the paywall header between the crown disc and the X: **Jotla Plus | Jotla AI**. Each side's selected pill wears its own live identity: Plus the purple gradient with white text, AI the premium navy gradient (`#14294A → #1E5099`) with gold (`#E6B85C`). Switching swaps the wordmark ("Jotla *AI*" with the AI mark italic in gold), the caption ("The deadlines, the rights and the letters, / kept current on your side."), and the carousel to the AI slides: EHCP and SEND deadline tracker, On-device AI help, Current letter templates, Rights kept current, Voice capture (all from the live tier's honest list; nothing already-shipping goes on it). **The AI tab shows real term cards plus the coming card, no CTA (Bupe, 6 Aug late).** The ladder is **£89 / 6 Months, £149 / One Year ("less than £13 a month")**, no monthly term, set 8 Aug on the first /arena verdict and live from build 2.0.5 (`decisions/log.md` 2026-08-08; the arena put every live AI-inclusive comparable at or under about £120/yr, so the 7 Aug £199 fell). It supersedes the 7 Aug £79 / £129 / £199 reset and the 6-Aug £59 / £99 / £149 first pass; neither is on screen anywhere. Below the cards sits the navy coming card: sparkle, "COMING IN 2027", "Jotla Plus is included in every term: one price, not one on top of another." Nothing is buyable before it exists, so there is no CTA; the AI year's gold-ringed Best value card anchors £79 Plus nicely meanwhile. The AI tab being visible at all is new: the live app hides the tier behind SHOW_AI_TIER. Prices are indicative until the 2027 launch passes the money gate.
- **The carousel art is real (6 Aug late).** Ten illustrations generated with Higgsfield (nano_banana_2, 2K, 16:9) to the vector-illustration playbook: semi-flat, layered tint-disc depth, single ground shadow, no text anywhere, object-led for the dysregulation slide, never a distressed child. Plus set wears the warm app palette; AI set wears navy and gold. Files live in `carousel/plus-1..5.png` and `carousel/ai-1..5.png`, wired to the dots and the tier selector. Full generation prompts are recorded in the git history of this file's commit.
- **The Plus page shows the launch state (Bupe, 6 Aug).** The header row keeps the crown disc (left), the tier selector (centre) and the close X (right). The wordmark lockup sits centred above the carousel: "Jotla" in Cal Sans at 31px with "+Plus" italic, purple, smaller (20px) and raised 7px so the pair sit together; under it "The tools to spot patterns / and make your case." on two lines, centred. The Family Sync disclaimer is gone: Family Sync sells as carousel slide 3 of 5 (Patterns and Month View, PDF Evidence Pack, Family Sync, Photos and Videos on Notes, Dysregulation Mode; the dots page the slides on the board). Launch condition, decisions log 2026-08-06: Family Sync ships working at launch or the slide comes out. Layout: the carousel (300 x 158 slot, centred text) floats vertically centred between the header and the pricing; the term cards pin directly above the CTA; then the free-included line, renewal text and links.
- **Today header order**: the greeting leads ("Good afternoon."), the date sits under it in the small caps overline, and the status line ("Here is how Sam's day is looking...") is gone.
- **Menu and Settings split (Bupe, 6 Aug): Menu holds what parents reach for; Settings, behind a cog, holds everything else.** The Todoist browse screen is the pattern (avatar and name top left, cog top right).
  - **Menu**: the screen title is the child, avatar plus name (tapping it opens the child page), with the **cog top right opening Settings**. **Jotla Plus is the first row and looks unique: a SOLID gold crown icon** (the only solid icon and the only gold in the app). Then Your record: **Backup and Restore** and **Recycle Bin** last (the live app's 15 Jul 30-day Bin, renamed, sub "Kept for 30 days"). The "Jotla by SEN Help" endorsement is the footer. Nothing else lives here.
  - **Settings** (pushed page): **Children first** (un-labelled top row, sub naming the children, opening the Children page), then Appearance (**Theme**, current value in grey, sheet with Light / Dark / System following the phone; **Text size**, sheet with Small / Standard / Large / Extra large: one step more than the live app's three), Privacy (**App lock · Off**), Reminders (**Daily reminder · Off**), Help and about (**Take the tour**, **Help**, **About Jotla**, **Tell us what you think**: the first and last already exist in the live app). No privacy footer here: the Backup page and About carry that promise (one home per fact).
  - **App lock and Daily reminder are new commitments adopted from the reference apps (both FREE, decided 6 Aug).** App lock follows the diary app's pattern re-aimed at Jotla's no-server promise: a child's record deserves a lock and privacy is never paywalled. Daily reminder is a gentle evening nudge to log the day: it drives the record habit, so it stays free like quick templates.
  - **Help and Support (designed 6 Aug late, both pages).** Help is a pushed page of questions that open in place (accordion rows): The record (where it lives, backups, new phone, Recycle Bin), Plus (cost, cancel, what a lapse keeps: the no-ransom list from source), Privacy (App lock), then Still stuck > Contact support. Every answer is verified against the app or tonight's designs, kept short, neutral and professional. Support is a pushed page with Email us (hello@sen.help), Tell us what you think, and one line: "Replies come from a real person at SEN Help." Nothing patronising anywhere; the reference apps' ticket systems, resource hubs and feedback-call bookings were deliberately not taken.
- **The Daily reminder sheet (designed 6 Aug):** a picker sheet from the Settings row, titled with the sub-line "A gentle nudge to write the day down." Options: **Off** (default) / **Morning · 08:00** / **Evening · 20:00** / **Choose a time**, which expands a Time field (the export sheet's expanding-custom pattern); a tap applies instantly and the row value tracks it (a chosen time shows as the bare time, e.g. "20:30"). Build note: the notification copy must never name the child or the record: lock screens are public.
  - **The App lock page (designed 6 Aug, shown switched on):** the lock toggle first ("Asked for every time Jotla opens"), then Unlock with (**Pattern or PIN** as a value row; **Fingerprint or face** toggle, sub "When your phone can"), then If you forget (**Security question · Not set yet**). The footer tells the local-only truth straight: "The lock, the pattern and your answer stay on this phone and are checked nowhere else. Jotla cannot reset a lock for you, so set the question." No recovery email (that would need servers), no crowns anywhere on the page.
- **Documents**: title is just "Documents" (the old subtitle and the green banner explained the screen, the declutter took both); segmented switch kept with **Documents leading** (Documents / Day records, swapped 2026-08-06); "Add a document" is a dashed row under the list.

## Tokens

### Dark (locked)

| Token | Value |
|---|---|
| bg | `#201F1D` |
| surface | `#282725` |
| row | `#2E2D2A` |
| hairline | `rgba(255,255,255,.07)` |
| text | `#EDEBE7` |
| muted | `#A6A29B` |
| faint | `#7B7770` |
| accent (icons, text, section headers) | `#7CA9EF` |
| accent fill (FAB, primary buttons) | `#2F6FD6` |
| accent tint / border | `rgba(124,169,239,.14)` / `rgba(124,169,239,.45)` |
| good / mixed / hard | `#52B788` / `#E8A33D` / `#E5645C` |
| dysregulation | `#B36AE2` (tint `.16`, border `.45`) |
| no-note | `#55524C` |
| gold (Plus crown only) | `#EBBA4D` |
| tile blue / tile purple (check-in tiles) | `rgba(47,111,214,.26)` / `rgba(179,106,226,.22)` |

### Light (first pass, in progress)

| Token | Value |
|---|---|
| bg | `#F7F5F2` (warm paper, replaces the old cool blue-white) |
| surface / row | `#FFFFFF` with border `rgba(35,33,29,.08)` |
| hairline | `rgba(35,33,29,.09)` |
| text | `#23211D` |
| muted | `#6F6A62` |
| faint | `#97928A` |
| accent + fill | `#1A56A8` (brand blue, full strength) |
| good / mixed / hard | `#1F9D66` / `#D9861C` / `#D64541` (numerals darker: `#1A7A50` / `#9A5E13` / `#A93832`) |
| dysregulation | `#9D34DA` (tint `.10`, border `.40`) |
| no-note | `#C9C4BC` |
| gold (Plus crown only) | `#BE8E1E` |
| tile blue / tile purple (check-in tiles) | `rgba(26,86,168,.15)` / `rgba(157,52,218,.14)` |

## What the build will touch (when green-lit)

`jotla.css` tokens and component styles, the tab bar (structure change: four tabs + FAB), the app header's removal, Find's relocation, and every screen's title block. Boot-assert will need its nav and header assertions re-anchored the same way the 4 Aug declutter pass re-anchored the wordmark ones: positive anchors first, then the negatives, with a negative control run before trusting green. Native mirrors after web per the two-track rule.
